const today=new Date()

const start=`${today.getFullYear()}-${today.getMonth()+1}-${today.getDay()}  ${today.getHours()}:${today.getMinutes()}`
console.log(start)


const data=`${today.getDay()}-${today.getMonth()+1}-${today.getFullYear()} ${today.getHours()}:${today.getMinutes()}`
console.log(data)

const monatoday=`${today.getDay()}/${today.getMonth()+1}/${today.getFullYear()} ${today.getHours()}:${today.getMinutes()}`
console.log(data)